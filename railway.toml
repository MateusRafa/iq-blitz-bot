[build]
builder = "NIXPACKS"

[deploy]
startCommand = "PYTHONPATH=. PYTHONUNBUFFERED=1 uvicorn web.app:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
